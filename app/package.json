{
  "name": "nimbus",
  "version": "1.0.0",
  "description": "Demo microservice for the complete DevOps (Terraform/Ansible/Jenkins/EKS/Prometheus) reference project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "jest --coverage",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.21.2",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.0.0",
    "eslint": "^9.13.0"
  },
  "jest": {
    "reporters": ["default", "jest-junit"]
  },
  "engines": {
    "node": ">=20"
  }
}
